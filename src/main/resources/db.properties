# =====================================================================
#  Database configuration - Sunrise Dental Clinic
#
#  XAMPP default settings: user "root" with an empty password.
#  This is a development-only assumption and is documented in the report.
#  For a real deployment, create a dedicated database user with limited
#  privileges and keep the credentials outside version control.
# =====================================================================

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/sunrise_dental?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Colombo&characterEncoding=UTF-8
db.username=root
db.password=
